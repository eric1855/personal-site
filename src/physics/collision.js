/**
 * Collision helpers.
 *
 * With the car now a dynamic Rapier body, the physics solver handles all
 * collision response automatically. This module only provides proximity
 * detection for UI prompts (distance-based, no physics needed).
 */

// (Proximity detection lives in main.js via _findNearest — nothing needed here.)
// This file is kept as a placeholder in case future collision event queries are needed.
